
# Generate a random password, if you include a number afterwards, the length of the password will
# be that long
function genpass { if [[ $1 > 0 ]]; then l=$1; else l=16; fi; < /dev/urandom tr -dc "[:graph:]" | head -c${1:-$l};echo; }
