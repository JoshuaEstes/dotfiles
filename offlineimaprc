; vi: set ft=dosini:
[general]
accounts = Personal, dSpaceLabs, AllyCommerce
; ui is one of machineui, syslog, quiet, ttyui, basic
;u           = machineui
;ui          = syslog
;ui          = quiet
ui          = ttyui
;ui          = basic
fsync       = False
socktimeout = 30
pythonfile  = ~/.offlineimap.py

[Account Personal]
localrepository  = Personal-Local
remoterepository = Personal-Remote

[Repository Personal-Local]
type         = Maildir
localfolders = ~/Mail/f1gm3nt@gmail.com
nametrans    = lambda folder: {
    'archive':   '[Gmail]/All Mail',
    'drafts':    '[Gmail]/Drafts',
    'sent':      '[Gmail]/Sent Mail',
    'starred':   '[Gmail]/Starred', }.get(folder, folder)

[Repository Personal-Remote]
maxconnections = 1
type           = Gmail
ssl            = True
sslcacertfile  = /usr/local/etc/openssl/cert.pem
remoteusereval = get_username('personal', '7D66AAB0')
remotepasseval = get_password('personal', '7D66AAB0')
realdelete     = no
readonly       = False
folderfilter   = lambda folder: folder not in ['[Gmail]/Important', '[Gmail]/Spam', '[Gmail]/Trash']
nametrans      = lambda folder: {
    '[Gmail]/All Mail':  'archive',
    '[Gmail]/Drafts':    'drafts',
    '[Gmail]/Sent Mail': 'sent',
    '[Gmail]/Starred':   'starred', }.get(folder, folder)

[Account dSpaceLabs]
localrepository  = dSpaceLabs-Local
remoterepository = dSpaceLabs-Remote

[Repository dSpaceLabs-Local]
type         = Maildir
localfolders = ~/Mail/joshua@dspacelabs.com
nametrans    = lambda folder: {
    'archive':   '[Gmail]/All Mail',
    'drafts':    '[Gmail]/Drafts',
    'sent':      '[Gmail]/Sent Mail',
    'starred':   '[Gmail]/Starred', }.get(folder, folder)

[Repository dSpaceLabs-Remote]
maxconnections = 1
type           = Gmail
ssl            = True
sslcacertfile  = /usr/local/etc/openssl/cert.pem
remoteusereval = get_username('dspacelabs', '479BDD6A')
remotepasseval = get_password('dspacelabs', '479BDD6A')
realdelete     = no
readonly       = False
folderfilter   = lambda folder: folder not in ['[Gmail]/Important', '[Gmail]/Spam', '[Gmail]/Trash']
nametrans      = lambda folder: {
    '[Gmail]/All Mail':  'archive',
    '[Gmail]/Drafts':    'drafts',
    '[Gmail]/Sent Mail': 'sent',
    '[Gmail]/Starred':   'starred', }.get(folder, folder)

[Account AllyCommerce]
localrepository  = AllyCommerce-Local
remoterepository = AllyCommerce-Remote

[Repository AllyCommerce-Local]
type         = Maildir
localfolders = ~/Mail/joshua.estes@allycommerce.com
nametrans    = lambda folder: {
        'archive': '[Gmail]/All Mail',
        'drafts':  '[Gmail]/Drafts',
        'sent':    '[Gmail]/Sent Mail',
        'starred': '[Gmail]/Starred',
        'trash':   '[Gmail]/Trash',
    }.get(folder, folder)

[Repository AllyCommerce-Remote]
maxconnections = 1
type           = Gmail
ssl            = True
sslcacertfile  = /usr/local/etc/openssl/cert.pem
remoteusereval = get_username('ally', 'A3C56024')
remotepasseval = get_password('ally', 'A3C56024')
realdelete     = no
readonly       = False
folderfilter   = lambda folder: folder not in [
        '[Gmail]/Important',
        '[Gmail]/Spam',
        '[Gmail]/Trash',
    ]
nametrans = lambda folder: {
        '[Gmail]/All Mail':  'archive',
        '[Gmail]/Drafts':    'drafts',
        '[Gmail]/Sent Mail': 'sent',
        '[Gmail]/Starred':   'starred',
        '[Gmail]/Trash':     'trash',
    }.get(folder, folder)
